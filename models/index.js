// used so server can point to all models in this folder -- import or require all model or all potential models for use

// not needed when working with only one model
module.exports = {
  Workout: require("./workout"),
};
